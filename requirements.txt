# Goodwill Gaming Platform v2.0 - Clean Requirements
# Core dependencies for enhanced platform with Maps & SQLite

fastapi>=0.100.0
uvicorn[standard]>=0.20.0
streamlit>=1.28.0
requests>=2.31.0
plotly>=5.17.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Maps functionality
folium>=0.15.0
streamlit-folium>=0.15.0

# Google ADK mock (since real package not available on PyPI)
# Using built-in libraries for notification agent